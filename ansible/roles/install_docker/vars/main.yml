---
packages: 
  - apt-transport-https
  - ca-certificates
  - curl
  - software-properties-common
  - python3-pip
  - virtualenv
  - python3-setuptools
  - gnupg
  - python-pkg-resources

docker_requirements:
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-compose-plugin

# apt_transport:
#   - apt-transport-https
#   - ca-certificates
#   - lsb-release
#   - gnupg

pip_install:
  - docker
  - requests
  - setuptools